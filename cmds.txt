# ssh
cd /tmp/pycharm_project_359

# install
pip install ipywidgets
pip install scikit-learn==0.22

# ipynb to py
jupyter-notebook

# train
python src/train.py --dir_afn_root /media/sdb/data/AffectNet
python -m torch.distributed.launch --nproc_per_node 8 src/train.py --dir_afn_root /media/sdb/data/AffectNet --batch 64 --device 0,1,2,3,4,5,6,7 --img_size 260

# test
python src/test.py